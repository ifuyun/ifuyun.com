export enum BookType {
  BOOK = 'book',
  MAGAZINE = 'magazine',
  NEWSPAPER = 'newspaper',
  JOURNAL = 'journal',
  PERIODICAL = 'periodical',
  OTHER = 'other'
}

export enum BookStatus {
  NORMAL = 1,
  TRASHED = 2
}
