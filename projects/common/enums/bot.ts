export enum BotScope {
  PUBLIC = 'public',
  UNLISTED = 'unlisted',
  PRIVATE = 'private'
}

export enum BotStatus {
  NORMAL = 'normal',
  TRASH = 'trash'
}

export enum BotFollowStatus {
  NORMAL = 'normal',
  TRASH = 'trash'
}

export enum CloseType {
  CLOSE = 0,
  CLOSE_AND_REFRESH = 1,
  CLOSE_AND_CHAT = 2
}
