export enum GameScope {
  PUBLIC = 'public',
  PRIVATE = 'private'
}

export enum GameStatus {
  NORMAL = 'normal',
  TRASH = 'trash'
}

export enum GameLogType {
  DOWNLOAD = 'download',
  PLAY = 'play'
}
