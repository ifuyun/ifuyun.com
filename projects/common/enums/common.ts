export enum Theme {
  Light = 'light',
  Dark = 'dark'
}

export enum ListMode {
  LIST = 'list',
  CARD = 'card'
}

export enum SwitchValue {
  YES = 1,
  NO = 0
}

export enum CommonStatus {
  NORMAL = 'normal',
  TRASH = 'trash'
}
