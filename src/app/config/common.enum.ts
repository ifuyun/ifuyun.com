export enum PostType {
  POST = 'post',
  PAGE = 'page'
}

export enum CommentFlag {
  OPEN = 'open',
  VERIFY = 'verify',
  CLOSE = 'close'
}

export enum VoteType {
  LIKE = 'like',
  DISLIKE = 'dislike'
}

export enum CopyrightType {
  FORBIDDEN = 0,
  AUTHORIZED = 1,
  CC = 2
}
