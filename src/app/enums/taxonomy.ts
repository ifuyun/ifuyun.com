export enum TaxonomyType {
  POST = 'post',
  BOT = 'bot',
  LINK = 'link'
}

export enum TaxonomyStatus {
  PUBLIC = 'public',
  PRIVATE = 'private',
  TRASH = 'trash'
}

export enum TagStatus {
  PUBLIC = 'public',
  PRIVATE = 'private',
  TRASH = 'trash'
}
